# InGen Studio — frontend environment
#
# NEXT_PUBLIC_ADAPTER_MODE controls the service backend:
#   mock  → all data lives in browser localStorage (default, no backend needed)
#   http  → POSTs YAML to the FastAPI service at NEXT_PUBLIC_API_BASE_URL
#
NEXT_PUBLIC_ADAPTER_MODE=http
NEXT_PUBLIC_API_BASE_URL=
